# M6 App Pass - 2026-07-01

Evidence screenshots were captured in `C:/Users/chris/AppData/Local/Temp/cobolens-audit-2026-07-01/`.

## Steps

1. Loaded `http://127.0.0.1:1420/?graph=/m6-bakeoff-graph.json`.
   Health: graph, code, filters, source tree, AI settings, and export controls rendered. The native light scrollbar in the left navigator made the menu feel visually odd.

2. Clicked a graph Ask shortcut for `What depends on LINEAGE?`.
   Health: instant graph answer worked with clickable citations. Full-page browser capture was unreliable for this fixed-height app, but viewport capture was correct.

3. Opened Summary and clicked `Explain from graph`.
   Health before fix: the action moved the user into Ask, making Summary and Ask feel blended. Health after fix: the cited graph explanation stays in Summary.

4. Clicked `Ask AI follow-up`.
   Health: Ask opens intentionally, pre-fills `Explain LINEAGE in plain English.`, and clearly labels the route as Ollama with citations.

5. Toggled filters, reset filters, searched symbols, clicked an external search result, checked AI readiness, clicked citations, and clicked export.
   Health: filters, search, external-node messaging, model readiness, and citations worked. Export updates status in browser preview, but the browser automation did not observe a download event, so desktop export should remain on the next manual QA list.

## Fixes From This Pass

- Renamed the inspector's first tab from `Overview` to `Summary`.
- Kept `Explain from graph` inside Summary as a deterministic cited graph explanation.
- Left `Ask AI follow-up` as the explicit handoff into chat.
- Added dark native scrollbar styling and adjusted inspector tab widths so badges do not clip labels.
- Updated the M6 UI contract smoke to cover the new behavior.
